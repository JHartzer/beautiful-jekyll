// .vscode/tasks.json
{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Deploy Jekyll",
            "command": "docker run --rm --volume=\"$PWD:/srv/jekyll\" -p 4000:4000 -it hartzerj_website jekyll serve --watch",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}